<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Accenture Template</title>
    <style>
        @page {
            size: 1600px 900px;
            margin: 0;
        }
        
        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Meiryo UI', 'Meiryo', sans-serif;
            background: #fff;
            color: #333;
            font-size: 12pt;
            line-height: 1.4;
        }
        
        .slide-container {
            width: 100%;
            height: 100vh;
            max-width: none;
            margin: 0;
            padding: 30px 30px 35px 30px;
            box-sizing: border-box;
            position: relative;
        }
        
        .main-title {
            font-size: 32pt;
            font-weight: 700;
            color: #333;
            margin-bottom: 18px;
            line-height: 1.2;
        }
        
        .accent-bar {
            width: 100%;
            height: 4px;
            background: linear-gradient(90deg, #a855f7 0%, #8b5cf6 50%, #7c3aed 100%);
            border-radius: 2px;
            margin-bottom: 15px;
        }
        
        .content-section {
            font-size: 18pt;
            color: #444;
            line-height: 1.5;
            margin-bottom: 25px;
        }
        
        .content-grid {
            display: grid;
            gap: 15px;
            margin: 15px 0;
            min-height: 320px;
        }
        
        /* 1-3 cards: 1 row */
        .content-grid[data-cards="1"] { grid-template-columns: 1fr; }
        .content-grid[data-cards="2"] { grid-template-columns: repeat(2, 1fr); }
        .content-grid[data-cards="3"] { grid-template-columns: repeat(3, 1fr); }
        
        /* 4-6 cards: 2 rows */
        .content-grid[data-cards="4"] { grid-template-columns: repeat(2, 1fr); grid-template-rows: repeat(2, 1fr); }
        .content-grid[data-cards="5"] { grid-template-columns: repeat(3, 1fr); grid-template-rows: repeat(2, 1fr); }
        .content-grid[data-cards="6"] { grid-template-columns: repeat(3, 1fr); grid-template-rows: repeat(2, 1fr); }
        
        /* 7-9 cards: 3 rows */
        .content-grid[data-cards="7"], 
        .content-grid[data-cards="8"], 
        .content-grid[data-cards="9"] { 
            grid-template-columns: repeat(3, 1fr); 
            grid-template-rows: repeat(3, 1fr);
            height: 450px;
        }
        
        /* 10-12 cards: 4 rows */
        .content-grid[data-cards="10"], 
        .content-grid[data-cards="11"], 
        .content-grid[data-cards="12"] { 
            grid-template-columns: repeat(3, 1fr); 
            grid-template-rows: repeat(4, 1fr);
            height: 520px;
        }
        
        /* Flexible Layout System (1x1 to 4x4) */
        /* 1 column layouts */
        .content-grid[data-layout="1x1"] { grid-template-columns: 1fr; grid-template-rows: 1fr; height: 250px; }
        .content-grid[data-layout="1x2"] { grid-template-columns: 1fr; grid-template-rows: repeat(2, 1fr); height: 380px; }
        .content-grid[data-layout="1x3"] { grid-template-columns: 1fr; grid-template-rows: repeat(3, 1fr); height: 480px; }
        .content-grid[data-layout="1x4"] { grid-template-columns: 1fr; grid-template-rows: repeat(4, 1fr); height: 580px; }
        
        /* 2 column layouts */
        .content-grid[data-layout="2x1"] { grid-template-columns: repeat(2, 1fr); grid-template-rows: 1fr; height: 250px; }
        .content-grid[data-layout="2x2"] { grid-template-columns: repeat(2, 1fr); grid-template-rows: repeat(2, 1fr); height: 350px; }
        .content-grid[data-layout="2x3"] { grid-template-columns: repeat(2, 1fr); grid-template-rows: repeat(3, 1fr); height: 450px; }
        .content-grid[data-layout="2x4"] { grid-template-columns: repeat(2, 1fr); grid-template-rows: repeat(4, 1fr); height: 550px; }
        
        /* 3 column layouts */
        .content-grid[data-layout="3x1"] { grid-template-columns: repeat(3, 1fr); grid-template-rows: 1fr; height: 250px; }
        .content-grid[data-layout="3x2"] { grid-template-columns: repeat(3, 1fr); grid-template-rows: repeat(2, 1fr); height: 320px; }
        .content-grid[data-layout="3x3"] { grid-template-columns: repeat(3, 1fr); grid-template-rows: repeat(3, 1fr); height: 420px; }
        .content-grid[data-layout="3x4"] { grid-template-columns: repeat(3, 1fr); grid-template-rows: repeat(4, 1fr); height: 520px; }
        
        /* 4 column layouts */
        .content-grid[data-layout="4x1"] { grid-template-columns: repeat(4, 1fr); grid-template-rows: 1fr; height: 250px; }
        .content-grid[data-layout="4x2"] { grid-template-columns: repeat(4, 1fr); grid-template-rows: repeat(2, 1fr); height: 350px; }
        .content-grid[data-layout="4x3"] { grid-template-columns: repeat(4, 1fr); grid-template-rows: repeat(3, 1fr); height: 450px; }
        .content-grid[data-layout="4x4"] { grid-template-columns: repeat(4, 1fr); grid-template-rows: repeat(4, 1fr); height: 550px; }
        
        .content-card {
            background: rgba(139, 92, 246, 0.05);
            border: 1px solid rgba(139, 92, 246, 0.2);
            border-radius: 8px;
            padding: 15px;
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
        }
        
        .card-title {
            font-size: 14pt;
            font-weight: 600;
            color: #a855f7;
            margin-bottom: 8px;
            display: block;
        }
        
        .card-content {
            font-size: 11pt;
            color: #555;
            line-height: 1.4;
            flex: 1;
        }
        
        /* Font scaling for many cards */
        .content-grid[data-cards="7"] .card-title,
        .content-grid[data-cards="8"] .card-title,
        .content-grid[data-cards="9"] .card-title { font-size: 12pt; }
        
        .content-grid[data-cards="7"] .card-content,
        .content-grid[data-cards="8"] .card-content,
        .content-grid[data-cards="9"] .card-content { font-size: 10pt; }
        
        .content-grid[data-cards="10"] .card-title,
        .content-grid[data-cards="11"] .card-title,
        .content-grid[data-cards="12"] .card-title { font-size: 11pt; }
        
        .content-grid[data-cards="10"] .card-content,
        .content-grid[data-cards="11"] .card-content,
        .content-grid[data-cards="12"] .card-content { font-size: 9pt; }
        
        /* Dynamic font scaling based on layout density */
        
        /* Large layouts (1-2 cards total) */
        .content-grid[data-layout="1x1"] .card-title,
        .content-grid[data-layout="2x1"] .card-title,
        .content-grid[data-layout="1x2"] .card-title { font-size: 18pt; }
        
        .content-grid[data-layout="1x1"] .card-content,
        .content-grid[data-layout="2x1"] .card-content,
        .content-grid[data-layout="1x2"] .card-content { font-size: 14pt; }
        
        /* Medium layouts (3-6 cards total) */
        .content-grid[data-layout="3x1"] .card-title,
        .content-grid[data-layout="2x2"] .card-title,
        .content-grid[data-layout="1x3"] .card-title,
        .content-grid[data-layout="3x2"] .card-title,
        .content-grid[data-layout="2x3"] .card-title { font-size: 14pt; }
        
        .content-grid[data-layout="3x1"] .card-content,
        .content-grid[data-layout="2x2"] .card-content,
        .content-grid[data-layout="1x3"] .card-content,
        .content-grid[data-layout="3x2"] .card-content,
        .content-grid[data-layout="2x3"] .card-content { font-size: 12pt; }
        
        /* Dense layouts (8+ cards total) */
        .content-grid[data-layout="4x1"] .card-title,
        .content-grid[data-layout="1x4"] .card-title,
        .content-grid[data-layout="4x2"] .card-title,
        .content-grid[data-layout="2x4"] .card-title,
        .content-grid[data-layout="3x3"] .card-title { font-size: 12pt; }
        
        .content-grid[data-layout="4x1"] .card-content,
        .content-grid[data-layout="1x4"] .card-content,
        .content-grid[data-layout="4x2"] .card-content,
        .content-grid[data-layout="2x4"] .card-content,
        .content-grid[data-layout="3x3"] .card-content { font-size: 10pt; }
        
        /* Very dense layouts (12+ cards total) */
        .content-grid[data-layout="3x4"] .card-title,
        .content-grid[data-layout="4x3"] .card-title,
        .content-grid[data-layout="4x4"] .card-title { font-size: 11pt; }
        
        .content-grid[data-layout="3x4"] .card-content,
        .content-grid[data-layout="4x3"] .card-content,
        .content-grid[data-layout="4x4"] .card-content { font-size: 9pt; }
        
        .card-content ul {
            margin: 0;
            padding-left: 0;
            list-style: none;
        }
        
        .card-content li {
            margin-bottom: 4px;
            padding-left: 12px;
            position: relative;
        }
        
        .card-content li:before {
            content: "•";
            color: #a855f7;
            position: absolute;
            left: 0;
        }
        
        .comparison-table {
            width: 100%;
            border-collapse: collapse;
            margin: 25px 0;
            background: white;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        
        .table-title {
            font-size: 16pt;
            font-weight: 600;
            color: #a855f7;
            margin-bottom: 12px;
            text-align: center;
        }
        
        .comparison-table th {
            background: linear-gradient(135deg, #8b5cf6 0%, #a855f7 100%);
            color: white;
            padding: 12px 10px;
            text-align: center;
            font-weight: 600;
            font-size: 12pt;
            border: 1px solid rgba(139, 92, 246, 0.3);
        }
        
        .comparison-table th:first-child {
            text-align: left;
            background: linear-gradient(135deg, #7c3aed 0%, #8b5cf6 100%);
        }
        
        .comparison-table td {
            padding: 10px;
            border: 1px solid rgba(139, 92, 246, 0.2);
            font-size: 11pt;
            text-align: center;
            vertical-align: middle;
        }
        
        .comparison-table td:first-child {
            font-weight: 600;
            color: #7c3aed;
            background: rgba(139, 92, 246, 0.05);
            text-align: left;
        }
        
        .comparison-table tr:nth-child(even) td:not(:first-child) {
            background: rgba(139, 92, 246, 0.02);
        }
        
        .notes-section {
            margin-top: 20px;
            font-size: 10pt;
            color: #666;
            line-height: 1.4;
        }
        
        .notes-section ul {
            margin: 0;
            padding-left: 15px;
        }
        
        .notes-section li {
            margin-bottom: 4px;
        }
        
        .footer-section {
            position: absolute;
            bottom: 10px;
            left: 30px;
            right: 30px;
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
        }
        
        .accenture-chevron {
            font-size: 32pt;
            color: #a855f7;
            font-weight: 900;
            font-family: 'Arial', sans-serif;
        }
        
        .copyright {
            color: #666;
            font-size: 10pt;
        }
        
        @media print {
            body {
                -webkit-print-color-adjust: exact;
                color-adjust: exact;
            }
        }
    </style>
</head>
<body>
    <div class="slide-container">
        {{> header}}
        
        <div class="content-grid" data-cards="{{cards.length}}"{{#if layout}} data-layout="{{layout}}"{{/if}}>
            {{#each cards}}
            <div class="content-card">
                <div class="card-title">{{title}}</div>
                <div class="card-content">
                    <ul>
                        {{#each items}}
                        <li>{{this}}</li>
                        {{/each}}
                    </ul>
                </div>
            </div>
            {{/each}}
        </div>

        {{#if table}}
        <div class="table-title">{{table.title}}</div>
        <table class="comparison-table">
            <thead>
                <tr>
                    {{#each table.headers}}
                    <th>{{this}}</th>
                    {{/each}}
                </tr>
            </thead>
            <tbody>
                {{#each table.rows}}
                <tr>
                    {{#each this}}
                    <td>{{this}}</td>
                    {{/each}}
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{/if}}

        {{#if notes}}
        <div class="notes-section">
            <ul>
                {{#each notes}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
        </div>
        {{/if}}

        {{> footer}}
    </div>
</body>
</html>